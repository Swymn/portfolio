import React from "react";

/**
 * Header component
 * 
 * @name Header
 * 
 * @returns {JSX.Element} Header component
 */
export const Header = (): JSX.Element => {
    return (
        <h1>
            Header
        </h1>
    )
}